package behavioral_command;

// the command object provides the method, execute(), that encapsulates the actions and can ben called to invoke the actions on the Receiver
public interface Command {	
	public void execute();	
}
